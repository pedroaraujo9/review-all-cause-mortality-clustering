% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_pca_fuzzy.R
\name{fit_pca_fuzzy}
\alias{fit_pca_fuzzy}
\title{Fit PCA-based fuzzy c-means clustering}
\usage{
fit_pca_fuzzy(data)
}
\arguments{
\item{data}{A data frame containing period life table data for multiple
countries. Must include columns \code{country}, \code{year}, \code{age},
and \code{qx}.}
}
\value{
A list with the following components:
\describe{
\item{\code{internal_metrics_plot}}{A \code{ggplot2} plot of internal
quality metrics (fuzzy silhouette, partition coefficient, and
Xie-Beni index) across 2 to 10 clusters.}
\item{\code{class_matrix}}{An integer matrix of hard cluster
assignments, with one column per number of clusters (1 to 10) and
one row per country.}
\item{\code{membership_level}}{A list of membership matrices (one per
number of clusters), each with one row per country and one column per
cluster.}
}
}
\description{
Applies a logit transformation to age-specific mortality rates \code{qx},
reduces dimensionality via PCA (retaining components that explain 90\\% of
variance), and then fits fuzzy c-means clustering on the principal component
scores. Internal quality metrics (fuzzy silhouette, partition coefficient,
and Xie-Beni index) are evaluated for 2 to 10 clusters.
}
\seealso{
\code{\link{fit_review_methods}}
}
